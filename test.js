require('dotenv').config()

// const clean = require('./src/actions/clean')
// clean().catch(e => console.error('Main error for clean', e))

/*

const { DRIVE_PIGES_FOLDER } = process.env
const { uploadFileAccordingPath } = require('./src/utils')

const folderPath = `${DRIVE_PIGES_FOLDER}/Radio FG/19-06-14`
const fileName = 'Radio FG@ven. 19-06-14 23.mp3'
const sourcePath = '/home/anthony/Downloads/Radio FG@ven. 19-06-14 23.mp3'
const mimeType = 'audio/mpeg'
uploadFileAccordingPath(folderPath, fileName, sourcePath, mimeType)
  .then(console.log)
  .catch(e => console.error('upload error for upload', e))

  */
